# Probe server dependencies (rpiPy/)
#
# NOTE: picamera2 is NOT installed via pip on Raspberry Pi OS — it ships as a
# system package. Install it with:  sudo apt install -y python3-picamera2
# (and run the server with system site-packages available). Everything below
# is pip-installable.

flask>=3.0
opencv-python>=4.8
numpy>=1.24
smbus2>=0.4          # INA219 / UPS monitor over I2C
requests>=2.31       # hardware.fire() -> score app POST /shot
python-dotenv>=1.0   # load SCORE_URL from .env
gpiozero>=2.0        # cart motor control (L298N) — falls back to mock off-Pi
# Pi-only pin backend for gpiozero on Raspberry Pi 5. Install on the Pi:
#   sudo apt install -y python3-lgpio   (or: pip install lgpio)
# Off-Pi it isn't needed — hardware.py mocks the drive when it can't init.
rpi-hardware-pwm>=0.2  # 38 kHz IR gun carrier (hardware PWM) — mocks off-Pi
# Needs the PWM overlay enabled once on the Pi (then reboot):
#   echo "dtoverlay=pwm" | sudo tee -a /boot/firmware/config.txt
