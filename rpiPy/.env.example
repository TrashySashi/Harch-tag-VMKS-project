# Copy to .env and set the score app address.
# This is the machine running scoreApp/score_server.py on port 5001.
# hardware.fire() POSTs to {SCORE_URL}/shot every time the probe fires.
SCORE_URL=http://127.0.0.1:5001

# Optional: HTTP timeout (seconds) for the shot report.
SCORE_TIMEOUT_S=3