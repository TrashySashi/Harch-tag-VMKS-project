# Copy to .env and set the probe (camera_stream) server address.
# This is the Raspberry Pi running camera_stream.py on port 5000.
PROBE_URL=http://10.62.210.69:5000

# Optional: HTTP timeout (seconds) for probe start/stop calls.
PROBE_TIMEOUT_S=3
